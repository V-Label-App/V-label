# Tasks - Profile & Me API

## Backend
- [ ] Create `auth.middleware.ts` (if missing) or verify it.
- [ ] Implement `UserController.getMe`.
- [ ] Setup `user.routes.ts`.
- [ ] Register `/api/v1/users` routes.

## Frontend
- [ ] Add `getCurrentUser` to `auth.api.ts` & update `AuthProvider`.
- [ ] Create `UserNav` component (Avatar + Name -> Dropdown -> Profile/Logout).
- [ ] Create `ProfilePage` UI (Detailed Identity + Role Stats).
- [ ] Add `/profile` route in `AppRoutes.tsx`.
- [ ] Implement Role-specific views in Profile.

## Verification
- [ ] Verify Token Persistence.
- [ ] Verify Logout clears state.
